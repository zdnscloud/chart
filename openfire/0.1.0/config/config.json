[{
        "label": "ingressDomain",
        "jsonKey": "ingress.host",
        "type": "string",
        "required": true,
        "description": "the ingress domain of lychee"
    },
    {
        "label": "storageClass",
        "jsonKey": "persistence.storageClass",
        "type": "enum",
        "required": false,
        "description": "lychee persistence storageClass and default is lvm",
        "validValues": ["cephfs", "lvm"]
    },
    {
        "label": "dataStorageSize",
        "jsonKey": "persistence.size",
        "type": "int",
        "required": false,
        "description": "lychee data persistence storage size and default is 10(Gi)"
    }
]