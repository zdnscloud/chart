[{
        "label": "rootUserPassword",
        "jsonKey": "rootUser.password",
        "type": "string",
        "required": false,
        "description": "MariaDB admin password and default is mariadb"
    },
    {
        "label": "slaveReplicas",
        "jsonKey": "slave.replicas",
        "type": "int",
        "required": false,
        "description": "slave replicas count and default is 2"
    },
    {
        "label": "storageClass",
        "jsonKey": "persistence.storageClass",
        "type": "storageClass",
        "required": false,
        "description": "mariadb persistence storageClass name"
    },
    {
        "label": "storageSize",
        "jsonKey": "persistence.size",
        "type": "int",
        "required": false,
        "description": "mariadb persistence storage size and default is 8(Gi)"
    },
    {
        "label": "serviceType",
        "jsonKey": "service.type",
        "type": "enum",
        "required": false,
        "description": "MariaDB service type[ClusterIP|NodePort] and default is ClusterIP",
        "validValues": ["ClusterIP", "NodePort"]
    }
]
